#include <stdio.h>

int main(void) {
    int varPar = 2, varImpar = 3;

    // Imprimimos los valores de las variables
    printf("El valor de la variable par es: %d\n", varPar);
    printf("El valor de la variable impar es: %d\n", varImpar);

    return 0;
}